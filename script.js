document.addEventListener('DOMContentLoaded', function() {
    // Elementos da interface
    const modal = document.getElementById('loginModal');
    const loginLink = document.getElementById('loginLink');
    const logoutBtn = document.getElementById('logoutBtn');
    const closeBtn = document.querySelector('.close');
    const loginForm = document.getElementById('loginForm');
    const errorMessage = document.getElementById('errorMessage');
    const areaPublica = document.getElementById('areaPublica');
    const areaProfissional = document.getElementById('areaProfissional');
    const userGreeting = document.getElementById('userGreeting');
    const profissionalNome = document.getElementById('profissionalNome');
    const formAgendamento = document.getElementById('formAgendamento');
    const loaderAgendamentos = document.getElementById('loaderAgendamentos');
    const statusAgendamentos = document.getElementById('statusAgendamentos');
    const tabelaAgendamentos = document.querySelector('#tabelaAgendamentos tbody');

    // Código do script omitido por brevidade
});